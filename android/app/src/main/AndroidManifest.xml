<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Voz (Fase 2). O `foregroundServiceType="microphone"` e a permissão
         correspondente são exigidos a partir do Android 14 (API 34); sem eles o
         serviço nem sobe. `POST_NOTIFICATIONS` é para a notificação persistente
         do serviço aparecer no Android 13+. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MenteDigital"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MenteDigital">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Risco R1 do plano: o Android suspende sockets de app em segundo
             plano e, sob Doze, desliga os rádios em janelas. Um serviço em
             primeiro plano é o ÚNICO jeito suportado de manter o microfone e o
             socket vivos de forma confiável — e é justificável para o usuário,
             porque a notificação diz "está ouvindo". -->
        <service
            android:name=".ServicoVoz"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>
</manifest>
