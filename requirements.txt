# --- Web / servidor ---
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
pydantic>=2.6
pydantic-settings>=2.2

# --- Núcleo IA ---
llama-cpp-python>=0.2.70      # compilar com suporte CUDA para a RTX 3080
                              # gate de bump (consultoria #12): antes de subir a versao,
                              # rode eval/retest_speculative.py numa env ISOLADA — o
                              # prompt-lookup crashava em contexto longo ate a 0.3.34
faster-whisper>=1.0          # STT (CTranslate2) — mesmos pesos do Whisper, mais rápido
piper-tts>=1.2.0
# XTTS-v2 (engine de voz GPU, opt-in via MENTE_TTS_ENGINE=xtts; código em tts_xtts.py,
# import tardio, default piper). O coqui-tts 0.27.5 quebra com transformers>=5
# ("isin_mps_friendly" foi removido), então FIXAMOS transformers em 4.57.x — o que também
# rebaixa huggingface-hub p/ 0.x. Validado neste stack: embeddings e5-base (sim 0.84),
# faster-whisper e a suíte (767) seguem OK — o projeto não usa transformers direto, só via
# sentence-transformers (que aceita >=4.41). NADA disto entra no requirements-ci.txt (leve).
coqui-tts>=0.27.5    # XTTS-v2 (fork Idiap). Baixe o modelo p/ dados/modelos/xtts_v2/
transformers>=4.57,<5   # travado <5 por causa do coqui/XTTS (ver acima)
num2words>=0.5.13    # verbalização de números PT-BR p/ o TTS (decimal/hora/moeda)
pillow>=10.0        # figuras dos livros -> WebP (Fase 5a); import tardio
pymupdf>=1.24        # extração de texto/TOC de PDF digital (scripts/ingerir_livro.py);
                     # import tardio — só o script de ingestão o carrega
numpy>=1.26

# --- RAG / VectorDB ---
langchain-core>=0.2
langchain-text-splitters>=0.2
langchain-chroma>=0.1.2
langchain-huggingface>=0.0.3
sentence-transformers>=2.7
chromadb>=0.5

# --- Busca web ---
ddgs>=6.0
httpx>=0.27          # deep-fetch async do corpo das páginas (não só snippets)
trafilatura>=1.8     # extração do texto principal (tira menu/rodapé/ads)
# DIRETA, não de carona: encyclopedia.py importa `from bs4 import BeautifulSoup`.
# Ela só estava chegando como dependência do trafilatura — no dia em que ele
# trocar de parser, a ingestão do livro web quebra sem nada ter mudado aqui.
beautifulsoup4>=4.12
