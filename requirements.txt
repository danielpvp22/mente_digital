# --- Web / servidor ---
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
pydantic>=2.6
pydantic-settings>=2.2

# --- Núcleo IA ---
llama-cpp-python>=0.2.70      # compilar com suporte CUDA para a RTX 3080
                              # gate de bump (consultoria #12): antes de subir a versao,
                              # rode eval/retest_speculative.py numa env ISOLADA — o
                              # prompt-lookup crashava em contexto longo ate a 0.3.34
faster-whisper>=1.0          # STT (CTranslate2) — mesmos pesos do Whisper, mais rápido
piper-tts>=1.2.0
# XTTS-v2 (engine de voz GPU, opt-in via MENTE_TTS_ENGINE=xtts): o código está pronto
# (tts_xtts.py, import tardio, default piper), MAS a instalação está BLOQUEADA neste
# stack — coqui-tts 0.27.5 quebra com transformers>=5 ("cannot import name
# isin_mps_friendly"), e o RAG/embeddings usa transformers 5.x. Por isso NÃO auto-instala
# aqui (evitar puxar um coqui inutilizável + torch pesado no CI). Para ativar: resolver o
# conflito de transformers (rebaixar p/ 4.x e revalidar embeddings, OU esperar release do
# coqui compatível), então:  pip install coqui-tts
# coqui-tts>=0.24    # (bloqueado: incompatível com transformers 5.x — ver acima)
num2words>=0.5.13    # verbalização de números PT-BR p/ o TTS (decimal/hora/moeda)
numpy>=1.26

# --- RAG / VectorDB ---
langchain-core>=0.2
langchain-text-splitters>=0.2
langchain-chroma>=0.1.2
langchain-huggingface>=0.0.3
sentence-transformers>=2.7
chromadb>=0.5

# --- Busca web ---
ddgs>=6.0
httpx>=0.27          # deep-fetch async do corpo das páginas (não só snippets)
trafilatura>=1.8     # extração do texto principal (tira menu/rodapé/ads)
